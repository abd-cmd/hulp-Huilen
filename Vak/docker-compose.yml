version: '3.8'
services:
#  mongodb:
#    container_name: mongodbvak_container
#    image: mongo
#    restart: always
#    volumes:
#      - data:/data
#    environment:
#      - MONGO_INITDB_ROOT_USERNAME=rootuser
#      - MONGO_INITDB_ROOT_PASSWORD=rootpass
#    ports:
#      - 27017:27017
#  mongodb-express:
#    container_name: mongoexpressvak_container
#    image: mongo-express
#    restart: always
#    ports:
#      - 8081:8081
#    environment:
#      - ME_CONFIG_MONGODB_ADMIN USERNAME=rootuser
#      - ME_CONFIG_MONGODB_ADMIN PASSWORD=rootpass
#      - ME_CONFIG_MONGODB_SERVER = mongodb
#      - ME_CONFIG_MONGODB_PORT = 27017
#
#volumes:
#  data: {}
  rabbitmq:
    restart: always
    image: rabbitmq:3-management
    ports:
     - "5672:5672"
     - "15672:15672"
    environment:
      RABBITMQ_NODENAME: bep3-rabbitmq-node

  mongodb:
      image: mongo
      restart: always
      ports:
       -  "27017:27017"

  mongo-express:
    image: mongo-express
    restart: always
    ports:
       - 9092:8081
    environment:
     ME_CONFIG_MONGODB_URL: mongodb://mongodb:27017/